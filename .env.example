# LLM
LLM_API_KEY=
LLM_MODEL=claude-sonnet-4-6
EMBEDDING_MODEL=text-embedding-3-small

# Database
DATABASE_URL=postgresql://askkmitl:askkmitl_dev@localhost:5432/askkmitl
POSTGRES_PASSWORD=askkmitl_dev

# Vector backend: pgvector | chroma
VECTOR_BACKEND=pgvector

# Security — generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
HASH_SALT=
SESSION_SECRET=

# App
APP_ORIGIN=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:8000
